{
  "name": "voicecontrol-monorepo",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @voicecontrol/web dev",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
